{
  "name": "product-finder",
  "version": "1.0.0",
  "description": "Portuguese supermarket product scraper with admin GUI",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate deploy",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.10.0",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "puppeteer-core": "^21.0.0",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.0",
    "prisma": "^5.10.0"
  },
  "prisma": {
    "schema": "prisma/schema.prisma"
  }
}
